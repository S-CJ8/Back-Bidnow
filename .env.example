# PostgreSQL (ajusta a tu entorno local)
DB_NAME=SUBASTA
DB_USER=postgres
# Obligatorio si el servidor exige contraseña (error: fe_sendauth: no password supplied)
DB_PASSWORD=tu_password
DB_HOST=localhost
DB_PORT=5432

# Opcional: DEBUG=True, SECRET_KEY=...
