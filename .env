# PostgreSQL (ajusta a tu entorno local)
DB_NAME=SUBASTA
DB_USER=postgres
# Obligatorio si PostgreSQL usa autenticación por contraseña (evita: fe_sendauth: no password supplied)
DB_PASSWORD="123456789"
DB_HOST=localhost
DB_PORT=5432

# Opcional: DEBUG=True, SECRET_KEY=...
